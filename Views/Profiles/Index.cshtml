@model IEnumerable<Profile>
@{
    ViewData["Title"] = "Profiles";
}
<h2 class="fw-semibold text-secondary">Profiles</h2>
<a asp-action="Create" class="btn btn-primary rounded-pill mr-1">Create New</a>
<div class="container-fluid">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Email</th>
                <th>FullName</th>
                <th>Date Of Birth</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var profile in Model)
            {
                <tr>
                    <td>@profile.Id</td>
                    <td>@profile.User?.Username</td>
                    <td>@profile.User?.Email</td>
                    <td>@profile.FullName</td>
                    <td>@profile.DateOfBirth.ToString("dd/MM/yyyy")</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@profile.Id" class="btn btn-outline-success btn-sm"><i class="bi bi-pencil-fill"></i> Edit</a>
                        <a asp-action="Delete" asp-route-id="@profile.Id" class="btn btn-outline-danger btn-sm"><i class="bi bi-trash3"></i> Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>